# LightTab 第一阶段：可靠发布

基于 1.19.0 / 9936332，2026-09-06。

产品方向：面向中文 AI 重度用户的轻量工作入口。第一批验证人群建议为每天使用多个 AI 工具的内容创作者；这是待验证假设。

## 本批修改

- 修正 MV3 CSP，只允许扩展包内脚本；搜索联想继续通过 host_permissions + fetch 工作。
- 删除错误要求远程脚本域名的测试断言。
- 壁纸来源发现从启动阶段移到用户打开壁纸设置后执行。
- 更新 README 和隐私说明，准确披露搜索联想、AI 提交、书签持久化、天气、WorkBuddy 回环探测和同步限制。
- 新增真实扩展安装检查，覆盖启动外部请求、模拟搜索联想、壁纸延迟请求和 chrome.storage 持久化。
- 修正现有 E2E 待办按钮选择器，统一使用 playwright 的浏览器与测试 API。

## 发布前尚未完成的必要工作

1. 生产同步联调：本批已补齐客户端的登录前备份、差异预览、按文档选择、备份下载与恢复；18 个模拟场景和真实扩展中的模拟云端流程通过。仍需用生产后端和两台真实设备验证 revision/CAS、删除记录及增量游标契约。
2. 云端删除：当前客户端明确返回不可用；需要后端删除接口和服务端数据生命周期验证。本仓库不包含后端实现。
3. AI 可靠性：逐一验证已登录、未登录、重定向、输入框改版和发送失败场景，提供清晰的复制后手动发送流程。模拟搜索测试不代表这些真实服务已通过验证。
4. 商店交付：准备商店素材、公开隐私页面、发布包和审核资料，完成 Chrome/Edge 安装验证。本批没有提交商店审核。
5. 性能：测量低配设备首次加载、重复打开、内存与输入响应，形成基线后再对外承诺“快”。

## 后续产品顺序

1. 提示词复用：变量填写、收藏、最近使用、版本记录；先服务一个高频任务。
2. 项目入口：把链接、提示词和下一步事项组织在同一个项目中，并可继续上次工作。
3. 小规模验证：邀请 20–30 位目标用户，观察数周内主动复用提示词和返回项目的行为。用户研究与数据收集应事先明确说明，默认不添加隐蔽埋点。

暂缓新增装饰组件、完整标签管理平台、广泛模型适配及 API 聚合。先验证留存，再设计同步/版本/共享等付费能力。

## 第二批：同步恢复

- 登录前保存本机备份，首次差异不再默认采用云端。
- 按文档暂停冲突，展示可读的两份内容，选择前重新核对云端版本；上传仍使用 baseRev，发生新冲突时不强制重试覆盖。
- 普通云端更新与删除也先备份。最近 3 份恢复备份仅存本机，不含登录凭据；下载格式兼容原有导入。
- 恢复前再次备份，并退出本机云同步，避免恢复结果立即上传或被云端改回。
- 保存备份失败时停止替换；可以下载并删除旧备份后重试。
- 本地保存、同步与恢复通过 Web Locks 排队，保护同步期间的编辑；请求有 10 秒超时。冲突按整份列表/设置处理，未实现逐条合并。
- 云端删除按钮显示为不可用，并说明仍缺服务端支持。

验证范围：18 个离线同步场景、真实 MV3 扩展中的模拟服务流程，以及原有页面 E2E；并未访问真实同步账号。后端及跨设备实机联调仍待完成。

## 本地验证命令

```sh
node scripts/smoke.cjs
node scripts/check-sync.cjs
npm install --no-save playwright
npx playwright install chromium
node scripts/check-extension.cjs
npx playwright test -c tests/e2e/playwright.config.js
```

真实扩展检查使用临时浏览器配置，并拦截页面 HTTP(S) 请求，不访问个人浏览器配置或真实 AI 账号。浏览器版本及商店审核结果仍需独立验证。
