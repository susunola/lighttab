# 1.24.2 remaining apply

Branch `v1.24.2-sea-holidays` already has:
- manifest 1.24.2
- CHANGELOG 1.24.2 header
- js/calendar.js local:// granted/declared

Apply locally:

```
git checkout v1.24.2-sea-holidays
git apply docs/1.24.2-sea-holidays.app.patch
```

Then copy `js/sea-holidays.js` from the 1.24.2 chrome zip, add the script tag in newtab.html, and the three i18n keys `cal.builtin_sea`, `cal.builtin_hint`, `cal.file_ics`.
